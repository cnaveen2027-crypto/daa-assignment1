# daa-assignment1
A Flask-based web application that interactively visualizes and compares the growth of common algorithmic time complexities (O(1), O(log n), O(n), O(n log n), and O(n²)) using Chart.js.
